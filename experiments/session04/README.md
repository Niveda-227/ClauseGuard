# Session 04 technical evidence

Generated by `python scripts/verify_sep22.py` on September 22, 2026. The bundled models were originally trained on September 10; this is a reproducibility run, not a fabricated training history.

- `verification.json`: actual timestamps, environment, data hash, selected artifact identity and scope.
- `test_results.txt`: executed automated tests; temporary mock trials are not participant evidence.
- `baseline/`, `balanced/`, `hybrid/`: validation metrics and per-sentence prediction records.
- `ERROR_NOTES.md`: four inspected source cases, annotation/task ambiguity and next experiments.
- `error_analysis.json`: per-category false positive/negative counts and source IDs.
- `structure_check.json`: exact report structure check, generated during packaging.
- `readiness_check.json`: missing-evidence check at packaging time; failure is expected until actual team/user facts are supplied.

No final test evaluation, user session, remote merge, approval or student authorship is established by these artifacts. Runtime values describe this environment, not every user's machine. Re-running the command updates technical outputs and timestamps; do not represent a later re-run as a pre-deadline event.
